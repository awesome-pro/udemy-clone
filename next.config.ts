import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "img-c.udemycdn.com", "s.udemycdn.com", "cms-images.udemycdn.com", "cdn0.desidime.com" ],
  },
};

export default nextConfig;
